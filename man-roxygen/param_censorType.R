#' @param censorType (`character(1)`)\cr
#' \describe{
#'   \item{`"cell count"`}{Censors pathways <`minFreq` to `minFreq`.}
#'   \item{`"remove"`}{Censors pathways <`minFreq` by removing them completely.}
#' }
